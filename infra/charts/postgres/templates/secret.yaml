apiVersion: v1
kind: Secret
metadata:
  name: postgres-secret
  namespace: metadata-db
type: Opaque
stringData:
  postgres-password: {{ .Values.postgres.password }}
  postgres-user: {{ .Values.postgres.user }}
  postgres-db: {{ .Values.postgres.database }}