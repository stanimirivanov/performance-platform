{
  "$schema": "http://json-schema.org/draft/2020-12/schema",
  "$id": "https://perfeng.dev/schemas/test-result.schema.json",
  "title": "Normalized Test Result",
  "description": "Normalized performance test result independent of tool",
  "type": "object",
  "required": ["schemaVersion", "runId", "metric", "distribution"],
  "properties": {
    "schemaVersion": {
      "type": "integer",
      "const": 1,
      "description": "Schema version"
    },
    "runId": {
      "type": "string",
      "pattern": "^perf-[0-9]{8}-[0-9]{6}-[a-f0-9]{8}$",
      "description": "Reference to the run metadata"
    },
    "metric": {
      "type": "object",
      "required": ["name", "direction"],
      "properties": {
        "name": {
          "type": "string",
          "description": "Metric name (e.g., checkout_duration_p95)"
        },
        "direction": {
          "type": "string",
          "enum": ["lower-is-better", "higher-is-better"],
          "description": "Whether lower or higher values are better"
        },
        "type": {
          "type": "string",
          "enum": ["latency", "throughput", "error_rate", "resource", "custom"],
          "description": "Metric type"
        },
        "unit": {
          "type": "string",
          "description": "Unit of measurement (ms, req/s, %, etc.)"
        }
      }
    },
    "distribution": {
      "type": "object",
      "required": ["samples"],
      "properties": {
        "samples": {
          "type": "integer",
          "minimum": 1,
          "description": "Number of samples"
        },
        "mean": {
          "type": "number",
          "description": "Arithmetic mean"
        },
        "median": {
          "type": "number",
          "description": "Median (p50)"
        },
        "p90": {
          "type": "number",
          "description": "90th percentile"
        },
        "p95": {
          "type": "number",
          "description": "95th percentile"
        },
        "p99": {
          "type": "number",
          "description": "99th percentile"
        },
        "stddev": {
          "type": "number",
          "description": "Standard deviation"
        },
        "min": {
          "type": "number",
          "description": "Minimum value"
        },
        "max": {
          "type": "number",
          "description": "Maximum value"
        },
        "cv": {
          "type": "number",
          "description": "Coefficient of variation"
        }
      }
    },
    "thresholds": {
      "type": "object",
      "description": "Threshold evaluation results",
      "properties": {
        "slo": {
          "type": "object",
          "description": "SLO threshold results",
          "additionalProperties": {
            "type": "object",
            "properties": {
              "passed": {
                "type": "boolean"
              },
              "threshold": {
                "type": "string"
              },
              "actual": {
                "type": "number"
              }
            }
          }
        },
        "regression": {
          "type": "object",
          "description": "Regression threshold results",
          "additionalProperties": {
            "type": "object",
            "properties": {
              "passed": {
                "type": "boolean"
              },
              "baselineValue": {
                "type": "number"
              },
              "actualValue": {
                "type": "number"
              },
              "percentChange": {
                "type": "number"
              }
            }
          }
        }
      }
    },
    "metadata": {
      "type": "object",
      "description": "Additional metadata",
      "additionalProperties": true
    }
  }
}
